version: '3.8'

services:
  matchzy-tournament:
    # Pull from Docker Hub (recommended - requires release first)
    image: sivertio/matchzy-auto-tournament:latest
    
    # If image not found, build locally:
    # Use docker/docker-compose.dev.yml instead:
    # docker compose -f docker/docker-compose.dev.yml up -d --build
    
    container_name: matchzy-tournament-api
    restart: unless-stopped
    ports:
      - '3069:3069' # Single port - Caddy serves frontend at / and proxies /api to backend
    environment:
      - NODE_ENV=production
      - PORT=3000 # Backend internal port (proxied by Caddy)
      - API_TOKEN=${API_TOKEN:-change-this-to-a-secure-token}
      - SERVER_TOKEN=${SERVER_TOKEN:-change-this-to-a-secure-server-token}
      - WEBHOOK_URL=${WEBHOOK_URL:-http://localhost:3069/api}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    volumes:
      # Persist SQLite database and demos
      - ./data:/app/data
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3069/health']
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - matchzy-network

networks:
  matchzy-network:
    driver: bridge

volumes:
  matchzy-data:
    driver: local
