############################################################
# Per-org Fragbase stack — copy to docker/env/<org-slug>.env
#
#   cp docker/example.env.org docker/env/camp-alpha.env
#   # edit STEAM_API_KEY + domains
#   ./scripts/org-stack.sh camp-alpha up
#
# Or generate with random secrets:
#   node scripts/generate-env.mjs
############################################################

##############################
# Stack identity (ORG_SLUG = filename slug)
##############################
ORG_SLUG=camp-alpha
ORGANIZATION_ID=camp-alpha
ORGANIZATION_NAME=Camp Alpha
ORGANIZATION_SLUG=camp-alpha
HOST_PORT=3070

##############################
# Public URLs — must match DNS / TLS
# FRONTEND_BASE_URL = org admin (OAuth redirects)
# API_BASE_URL = API (CS2 webhooks, Mercado Pago)
# PLAYER_PORTAL_URL = player portal (checkout returns)
##############################
FRONTEND_BASE_URL=https://admin.camp-alpha.fragbase.gg
API_BASE_URL=https://admin.camp-alpha.fragbase.gg
PLAYER_PORTAL_URL=https://play.camp-alpha.fragbase.gg

##############################
# Caddy vhosts (match DNS)
##############################
ORG_HOST=admin.camp-alpha.fragbase.gg
PLAYER_HOST=play.camp-alpha.fragbase.gg

##############################
# Database (isolated per org)
##############################
DB_USER=postgres
DB_PASSWORD=change-me-use-openssl-rand-base64-32
DB_NAME=matchzy_tournament

##############################
# Runtime
##############################
NODE_ENV=production
PORT=3000
SESSION_SECRET=change-me-use-openssl-rand-base64-32
SERVER_TOKEN=change-me-shared-with-cs2-matchzy-plugin

##############################
# Auth
##############################
AUTH_STEAM_ENABLED=true
STEAM_API_KEY=

# Optional SSO — redirect = FRONTEND_BASE_URL + /api/auth/<provider>/callback
# AUTH_DISCORD_ENABLED=true
# DISCORD_CLIENT_ID=
# DISCORD_CLIENT_SECRET=
# AUTH_GITHUB_ENABLED=true
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# AUTH_KEYCLOAK_ENABLED=true
# KEYCLOAK_ISSUER_URL=
# KEYCLOAK_CLIENT_ID=
# KEYCLOAK_CLIENT_SECRET=

##############################
# Mercado Pago (if registration fee > 0)
##############################
MERCADOPAGO_CLIENT_ID=
MERCADOPAGO_CLIENT_SECRET=
MERCADOPAGO_REDIRECT_URI=https://admin.camp-alpha.fragbase.gg/api/payments/mercadopago/callback

##############################
# Docker & logging
##############################
# MAT_IMAGE=sivertio/matchzy-auto-tournament:latest
LOG_LEVEL=info
# CORS_ORIGIN=*

##############################
# Optional
##############################
# FACEIT_API_KEY=
# CS2_ADMINS_JSON_PATH=
# MATCHZY_ENABLE_SIMULATION_IN_PROD=false
