############################################################
# Per-org Fragbase stack — copy to docker/env/<org-slug>.env
#
#   cp docker/example.env.org docker/env/camp-alpha.env
#   ./scripts/org-stack.sh camp-alpha up
############################################################

# Identity (must match filename / ORG_SLUG)
ORG_SLUG=camp-alpha
ORGANIZATION_NAME=Camp Alpha
ORGANIZATION_SLUG=camp-alpha

# Unique port on the host when multiple org stacks share one VPS
HOST_PORT=3070

# Public URLs (Mercado Pago webhook + checkout return URLs)
FRONTEND_BASE_URL=https://admin.camp-alpha.fragbase.gg
API_BASE_URL=https://admin.camp-alpha.fragbase.gg
PLAYER_PORTAL_URL=https://play.fragbase.gg

# Caddy hostnames inside the container (match your DNS / tunnel)
ORG_HOST=admin.camp-alpha.fragbase.gg
PLAYER_HOST=play.camp-alpha.fragbase.gg

########################
# Database (isolated per org)
########################
DB_USER=postgres
DB_PASSWORD=change-me-strong-password
DB_NAME=matchzy_tournament

########################
# Auth & integrations
########################
SESSION_SECRET=change-me-use-openssl-rand-base64-32
SERVER_TOKEN=change-me-shared-with-cs2-matchzy-plugin

STEAM_API_KEY=

# Mercado Pago (this org's checkout)
MERCADOPAGO_CLIENT_ID=
MERCADOPAGO_CLIENT_SECRET=
MERCADOPAGO_REDIRECT_URI=https://admin.camp-alpha.fragbase.gg/api/payments/mercadopago/callback

########################
# Optional
########################
# LOG_LEVEL=info
