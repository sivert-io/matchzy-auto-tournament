# ========================================
# MatchZy Auto Tournament - Environment Variables
# ========================================

# ============ REQUIRED ============

# API Token for admin authentication (Web UI + API endpoints)
# Generate a secure random string (e.g., openssl rand -hex 32)
API_TOKEN=your-secure-admin-token-here

# Server Token for MatchZy webhook authentication
# Must match the token configured in MatchZy plugin
SERVER_TOKEN=your-secure-server-token-here

# Webhook URL where CS2 servers send MatchZy events
# This is the URL that CS2 servers will use to reach your API
# Examples:
#   - Same machine: http://localhost:3000
#   - Same network: http://192.168.1.100:3000
#   - Production: https://yourdomain.com
WEBHOOK_URL=http://localhost:3000


# ============ OPTIONAL ============

# Steam API Key for vanity URL resolution (optional but recommended)
# Get your free key at: https://steamcommunity.com/dev/apikey
# STEAM_API_KEY=your-steam-api-key-here

# Server port (default: 3000)
# Note: In Docker, this is the internal Express port. Caddy serves on port 3069.
# PORT=3000

# Node environment (development | production)
# NODE_ENV=development

# Logging level (info | debug)
# LOG_LEVEL=info

# CORS origin for Socket.IO (default: *)
# Set to your frontend URL for production security
# CORS_ORIGIN=http://localhost:5173


# ============ DOCKER NOTES ============
# Docker architecture:
#   - Internal Caddy listens on port 3069 (exposed to host)
#   - Caddy serves frontend at / and proxies /api to Express
#   - Express runs internally on port 3000 (not exposed)
#   - Single port access: http://localhost:3069
# 
# For SSL in production, use an external reverse proxy (nginx/Caddy)
# and set WEBHOOK_URL to your HTTPS domain for webhook callbacks.
