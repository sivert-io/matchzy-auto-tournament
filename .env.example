# ==============================================================================
# MatchZy Auto Tournament - Environment Variables
# ==============================================================================

# ============== REQUIRED ==============

# API Token for admin authentication (Web UI + protected API endpoints)
# Generate a secure random string (e.g., `openssl rand -hex 32`)
API_TOKEN=your-secure-admin-token-here

# Server Token used by CS2 servers when calling webhook endpoints
# Generate another unique token and configure it in MatchZy
SERVER_TOKEN=your-secure-server-token-here

# ============== DATABASE CONFIGURATION (PostgreSQL Required) ==============

# PostgreSQL is required for all setups (Docker and local development)
# For local development, use: yarn db
# This will start a PostgreSQL container automatically.
# Or run manually: docker run -d --name matchzy-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=matchzy_tournament -p 5432:5432 postgres:16-alpine

# PostgreSQL connection settings
# For local development: use localhost (if running yarn db)
# For Docker Compose: use 'postgres' as host (service name)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=matchzy_tournament

# Alternative: Use DATABASE_URL instead of individual DB_* variables
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/matchzy_tournament

# ============== OPTIONAL ==============

# API port (default: 3000). Usually only overridden for local development.
# PORT=3000

# Log level for the backend (info | debug | warn | error). Defaults to info.
# LOG_LEVEL=info

# NOTE:
#   Webhook URL and Steam API key are now managed from the in-app Settings page.
#   Make sure to set them after the application starts so matches and Steam
#   lookups work correctly.